

```bash
# in root folder
rm -rf target native-image/config

# This will generate config for native image
# A window will pop up. Exercise the window and the close
# config saved to native-image/config
./native-image/config.sh

# build the native image executable
./native/image/compile.sh

```
